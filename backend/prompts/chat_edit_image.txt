You are editing an existing nutra ad creative based on a user instruction.

You receive:
- The current creative image (first reference).
- The product image to preserve (second reference).
- An instruction from the user (any language).

Apply the instruction while:
- Preserving the product packaging EXACTLY as in reference 2 (label, colors, shape).
- Preserving readability of all text already on the creative (unless the instruction explicitly asks to change copy).
- Preserving the overall nutra-genre aesthetic.
- Following the same content rules:
  - NO real celebrities or named public figures.
  - NO real third-party brand logos.
  - NO graphic medical close-ups (stylize, no realistic pathology).
  - All claims soft ("improves", "supports") — never hard ("cures", "guarantees").

EDIT INSTRUCTION:
{user_instruction}

OUTPUT: a single edited Facebook ad creative.
